name: lingua_units
description: Library for internal units localization.
version: 0.0.4
repository: https://github.com/tyrcord/lingua/tree/main/packages/units

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  easy_localization: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  lingua_core: ^0.0.31
