name: lingua_finance_dividend
description: Library for internal finance dividend localization.
version: 0.0.3
repository: https://github.com/tyrcord/lingua/tree/main/packages/finance_dividend

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  easy_localization: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  lingua_core: ^0.0.23
