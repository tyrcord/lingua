name: lingua_calculator
description: Library for internal calculator localization.
version: 0.0.4
repository: https://github.com/tyrcord/lingua/tree/main/packages/calculator

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  easy_localization: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  lingua_core: ^0.0.31
